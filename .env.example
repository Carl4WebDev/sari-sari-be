# Server Configuration
PORT=5000
NODE_ENV=development
APP_URL=http://localhost:5000

# Security / JWT (Must be at least 32 characters)
JWT_SECRET=listahub_super_secret_jwt_key_2026_dev_32chars

# CORS (Comma-separated allowed frontend origins)
ALLOWED_ORIGINS=http://localhost:5173,http://localhost:3000,http://localhost:5000,http://127.0.0.1:5173,http://127.0.0.1:3000

# Database Connection (Option 1: PostgreSQL Connection URL - recommended for Supabase / Neon / Render)
# DATABASE_URL=postgresql://postgres:password@localhost:5432/sarisari_db

# Database Connection (Option 2: Individual Parameters - for local PostgreSQL)
DB_HOST=localhost
DB_PORT=5432
DB_USER=postgres
DB_PASS=postgres
DB_NAME=sarisari_db

# Push Notifications (Web Push VAPID keys)
VAPID_PUBLIC_KEY=BDZ01uTxOClEgk2nv0qHcXRqoXXfydsUN47Z2sZvQEewGlytvuhItUiJAOnMFcGdVnbtp9fbHUOUJ77sl5gCkcE
VAPID_PRIVATE_KEY=eNib61z5v7DAorfN12lk1tk6PkHte9FAADpNClOdd5M
VAPID_SUBJECT=mailto:admin@listahub.net

# SMS Provider (Semaphore - Optional for dev)
SEMAPHORE_API_KEY=
SEMAPHORE_SENDER_NAME=Listahub
